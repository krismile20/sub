name: Build LG V30 Kernel

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          repository: TosteRino/joan-kernelsu
          path: joan-kernelsu

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git wget curl xz-utils \
            build-essential ccache \
            libncurses5-dev libssl-dev libelf-dev \
            bison flex bc python3 \
            clang lld llvm \
            gcc-aarch64-linux-gnu \
            gcc-arm-linux-gnueabi

      - name: Enable ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: joan-kernelsu-ccache
          max-size: 5G

      - name: Apply Droidspaces patches
        working-directory: joan-kernelsu
        run: |
          wget -q -O d1.patch https://raw.githubusercontent.com/ravindu644/Droidspaces-OSS/main/Documentation/resources/kernel-patches/non-GKI/01.fix_kernel_panic_in_xt_qtaguid.patch
          curl -sL -o d2.patch "https://raw.githubusercontent.com/ravindu644/Droidspaces-OSS/main/Documentation/resources/kernel-patches/non-GKI/02.fix_restore%20cgroup%20file%20prefix%20handling%20.patch"
          patch -p1 < d1.patch
          patch -p1 < d2.patch

      - name: Create Droidspaces config fragment
        working-directory: joan-kernelsu
        run: |
          cat > arch/arm64/configs/droidspaces.config << 'CONFIGEOF'
CONFIG_SYSCTL=y
CONFIG_SYSVIPC=y
CONFIG_POSIX_MQUEUE=y
CONFIG_NAMESPACES=y
CONFIG_IPC_NS=y
CONFIG_PID_NS=y
CONFIG_UTS_NS=y
CONFIG_USER_NS=y
CONFIG_NET_NS=y
CONFIG_CGROUPS=y
CONFIG_CGROUP_DEVICE=y
CONFIG_CGROUP_PIDS=y
CONFIG_CGROUP_FREEZER=y
CONFIG_CGROUP_CPUACCT=y
CONFIG_CGROUP_SCHED=y
CONFIG_FAIR_GROUP_SCHED=y
CONFIG_CGROUP_NET_PRIO=y
CONFIG_MEMCG=y
CONFIG_CPUSETS=y
CONFIG_DEVTMPFS=y
CONFIG_DEVTMPFS_MOUNT=y
CONFIG_VETH=y
CONFIG_BRIDGE=y
CONFIG_BRIDGE_NETFILTER=y
CONFIG_NETFILTER=y
CONFIG_NETFILTER_ADVANCED=y
CONFIG_NF_CONNTRACK=y
CONFIG_NF_NAT=y
CONFIG_NF_TABLES=y
CONFIG_IP_NF_IPTABLES=y
CONFIG_IP_NF_FILTER=y
CONFIG_IP_NF_NAT=y
CONFIG_IP_NF_TARGET_MASQUERADE=y
CONFIG_NETFILTER_XT_TARGET_MASQUERADE=y
CONFIG_NETFILTER_XT_TARGET_TCPMSS=y
CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
CONFIG_NETFILTER_XT_MATCH_CONNTRACK=y
CONFIG_NETFILTER_XT_MATCH_STATE=y
CONFIG_NF_CONNTRACK_NETLINK=y
CONFIG_NF_NAT_REDIRECT=y
CONFIG_IP_ADVANCED_ROUTER=y
CONFIG_IP_MULTIPLE_TABLES=y
CONFIG_NF_CONNTRACK_IPV4=y
CONFIG_NF_NAT_IPV4=y
CONFIG_OVERLAY_FS=y
CONFIG_EXT4_FS=y
CONFIG_EXT4_FS_POSIX_ACL=y
CONFIG_TMPFS=y
CONFIG_TMPFS_XATTR=y
CONFIG_TMPFS_POSIX_ACL=y
CONFIG_FW_LOADER=y
CONFIG_FW_LOADER_USER_HELPER=y
CONFIG_SECCOMP=y
CONFIG_SECCOMP_FILTER=y
CONFIGEOF

      - name: Merge config
        working-directory: joan-kernelsu
        run: |
          ./scripts/kconfig/merge_config.sh -m -O out \
            arch/arm64/configs/lineageos_joan_defconfig \
            arch/arm64/configs/droidspaces.config
          make ARCH=arm64 O=./out olddefconfig

      - name: Build kernel
        working-directory: joan-kernelsu
        run: |
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export CLANG_TRIPLE=aarch64-linux-gnu-
          make ARCH=arm64 O=./out CC=clang REAL_CC=clang olddefconfig
          make ARCH=arm64 O=./out \
            CC=clang \
            REAL_CC=clang \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            Image.gz-dtb -j$(nproc --all) 2>&1 | tee build.log

      - name: Check artifacts
        working-directory: joan-kernelsu
        run: |
          ls -lh out/arch/arm64/boot/
          file out/arch/arm64/boot/Image.gz-dtb

      - name: Package AnyKernel3
        working-directory: joan-kernelsu
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git
          cp out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
          cd AnyKernel3
          rm -rf .git
          cat > anykernel.sh << 'EOF'
kernel.string=LG V30 Stable KSU-Next + Droidspaces
do.devicecheck=1
device.name1=joan
device.name2=H930
device.name3=H930DS
device.name4=US998
block=/dev/block/bootdevice/by-name/boot
is_slot_device=0
ramdisk_compression=gzip
patch_vbmeta_flag=auto
EOF
          zip -r9 "../LG-V30-Stable-KSU-Droidspaces.zip" ./*
          cd ..

      - name: Upload flashable zip
        uses: actions/upload-artifact@v4
        with:
          name: LG-V30-Flashable-ZIP
          path: joan-kernelsu/LG-V30-Stable-KSU-Droidspaces.zip
          if-no-files-found: error

      - name: Upload raw kernel image
        uses: actions/upload-artifact@v4
        with:
          name: LG-V30-Raw-Image.gz-dtb
          path: joan-kernelsu/out/arch/arm64/boot/Image.gz-dtb

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: joan-kernelsu/build.log
